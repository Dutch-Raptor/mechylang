// Generate a lot of memory

fn memory_hog(n) {
  for i in (0..n) {
    let big_array = []
    println(i, " ")
    for j in (0..1000) {
      big_array.push("Some string that is large")
      big_array.push("Another string but as an array".iter().collect())
    }
    / Do nothing with this
  }
}


print("Hello World!")
memory_hog(1000)